---
sabnzbd:
  build: .
  container_name: sabnzbd
  volumes:
    - /opt/ejson/keys:/opt/ejson/keys:ro
    - /mnt/docker/sabnzbd:/data
    - /mnt/media:/media
  cap_add:
    - SYS_ADMIN
  user: root
  stdin_open: true
  tty: true
